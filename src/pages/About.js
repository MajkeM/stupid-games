
export default function About({ addPoint }) {
    return (
        <div>
            <button onClick={addPoint}>Add Point</button>
            <h1>About</h1>
            <p>This is the about page</p>
        </div>
    )
}